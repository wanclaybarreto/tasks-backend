spring:
  application:
    name: tasks-backend
  h2:
    console:
      enabled: true
      path: /h2-console # URL de acesso: localhost:8080/h2-console
      settings:
        trace: false
        web-allow-others: false
